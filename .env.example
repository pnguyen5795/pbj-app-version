# Copy to .env and fill in your real key.
# Dashboard -> API Keys -> Publishable key. Safe to expose to the browser
# (that's what it's for) — the matching secret key lives in server/.env.example.
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
